import 'package:flutter/material.dart';

class KonselingView extends StatelessWidget {
  const KonselingView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: const Text('Konseling tab'),);
  }
}
